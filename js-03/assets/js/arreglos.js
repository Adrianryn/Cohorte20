const numeros = [2,4,6,57,200];
let suma = 0 

/* numeros.forEach(function sumar(elemento){
suma += elemento;
console.log(suma);
    
}); */

numeros.forEach((el, indice) =>{
    console.log(el, indice);
});
/* map */
/* genera un copia del arreglo original */

let nuevoArr = numeros.map(())